server:
  port: 8080

eureka:
  client:
    enabled: false
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: ordering-service
  config:
    activate:
      on-profile: local
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 2000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/careup
    username: root
    password: 1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MariaDBDialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show_sql: true
  servlet:
    multipart:
      enabled: true
      max-file-size: 1000MB
      max-request-size: 1000MB
  kafka:
    enabled: false  # Kafka 비활성화
    bootstrap-servers: localhost:9092
    consumer:
      group-id: ordering-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "com.careup.ordering,com.careup.branch"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USER}
    password: ${MAIL_PW}
    from: careup.noreply@gmail.com
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true

  main:
    allow-bean-definition-overriding: true

cloud:
  aws:
    credentials:
      access-key: ${ACCESS_KEY}
      secret-key: ${SECRET_KEY}
    region:
      static: ap-northeast-2
    s3:
      bucket: ${S3_BUCKET}

# ========== 토스 결제 설정 ⭐ ==========
payment:
  toss:
    client-key: test_ck_Z61JOxRQVEE6gXGaDjv2VW0X9bAq    # 클라이언트 키
    secret-key: test_sk_P9BRQmyarYleRRplKdkZVJ07KzLN    # 시크릿 키 (Base64 인코딩 필요)
    security-key: 9e164b0107777969ccefd888fe6f416b949c04bd051c9a07ff69f6ab38fe680c  # 보안키
    api-url: https://api.tosspayments.com  # 토스 API URL


jwt:
  accessTokenExpiryMinutes: 30
  refreshTokenExpiryDaysPersistent: 180
  refreshTokenExpiryDaysNonPersistent: 1
  secretKeyAt: ${ACCESS_TOKEN_CUSTOMER}
  secretKeyRt: ${REFRESH_TOKEN_CUSTOMER}

security:
  csrf:
    allowed-origins:
      - "http://localhost:3000"
    skip-paths:
      - "/health"

careup:
  mail:
    customer:
      reset-base-url: "http://localhost:3000/reset-password"

logging:
  level:
    root: info

